import * as React from 'react';
import { GridRowId } from '../models/gridRows';
export interface GridRowProps {
    id: GridRowId;
    selected: boolean;
    rowIndex: number;
    children: React.ReactNode;
}
export declare function GridRow(props: GridRowProps): JSX.Element;
